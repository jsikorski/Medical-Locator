<phone:PhoneApplicationPage xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            xmlns:Converters="clr-namespace:MedicalLocator.Mobile.Converters"
                            x:Class="MedicalLocator.Mobile.Features.LoginPage"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}"
                            SupportedOrientations="Portrait"
                            Orientation="Portrait"
                            mc:Ignorable="d"
                            d:DesignHeight="768"
                            d:DesignWidth="480"
                            shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot"
          Background="Transparent">
        <Grid.Resources>
            <Converters:MyBooleanToVisibilityConverter x:Key="myBooleanToVisibilityConverter" />
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:Pivot Title="pivot"
                        Grid.Row="1">
            <controls:PivotItem Header="login">
                <ScrollViewer>
                    <StackPanel Height="607"
                                Width="456">
                        <toolkit:ToggleSwitch x:Name="toggleSwitch"
                                              Header="Login anonymously"
                                              Height="112" IsChecked="True" />
                        <TextBlock Text="Login"
                                   Margin="8,0"
                                   Visibility="{Binding IsChecked, ElementName=toggleSwitch, Mode=OneWay, Converter={StaticResource myBooleanToVisibilityConverter}, ConverterParameter=true}" />
                        <TextBox Margin="8,0"
                                 TextWrapping="Wrap"
                                 Height="72"
                                 Visibility="{Binding IsChecked, ElementName=toggleSwitch, Mode=OneWay, Converter={StaticResource myBooleanToVisibilityConverter}, ConverterParameter=true}" />
                        <TextBlock Text="Password"
                                   Margin="8,0"
                                   Visibility="{Binding IsChecked, ElementName=toggleSwitch, Mode=OneWay, Converter={StaticResource myBooleanToVisibilityConverter}, ConverterParameter=true}" />
                        <PasswordBox Height="72"
                                     Margin="8,0"
                                     Visibility="{Binding IsChecked, ElementName=toggleSwitch, Mode=OneWay, Converter={StaticResource myBooleanToVisibilityConverter}, ConverterParameter=true}" />
                        <Button x:Name="OpenMainPage"
                                Content="Login" Margin="0" />
                        <Grid Margin="0,0,174,0" />
                        <Grid Height="131"
                              Width="426" />
                    </StackPanel>
                </ScrollViewer>
            </controls:PivotItem>
            <controls:PivotItem Header="item2">
                <ScrollViewer />
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
</phone:PhoneApplicationPage>
