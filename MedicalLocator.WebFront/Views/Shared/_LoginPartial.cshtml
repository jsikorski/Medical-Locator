@if (User.Identity.IsAuthenticated)
{
    @:Welcome @User.Identity.Name!
        @Html.ActionLink("Logout", "Logout", "Users", new { id = "login_button", @class = "menu_item" });
}
else
{
    @Html.ActionLink("Login", "Login", "Users", new { id = "login_button", @class = "menu_item" })
}
