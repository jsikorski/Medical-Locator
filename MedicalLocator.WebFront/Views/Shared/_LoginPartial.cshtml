<div id="login_block" class="menu_item">
    @if (User.Identity.IsAuthenticated)
    {
        @Html.ActionLink(User.Identity.Name, "ShowUserPopup", "Users");
    }
    else
    {
        @Html.ActionLink("Login", "Login", "Users")
    }
</div>
